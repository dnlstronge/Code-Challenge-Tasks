

const generateTarget = () => {
  return Math.floor(Math.random()* 9 + 1)}

const compareGuesses = (userGuess, computerGuess, generateTarget) => { 
  if (Math.abs(generateTarget - userGuess) <= Math.abs(generateTarget - userGuess)) {
    return true }
    else {
   return false 
  }
}
updateScore = (winner) => {
  if (winner = 'human') {
    let humanScore = +1
  } else {
  let computerScore = +1
  }
}

let humanScore = 0;
let computerScore = 0;
let currentRoundNumber = 1;

const advanceRound = (currentRoundNumber) => {
  if (currentRoundNumber > 1)
  {let currrentRoundNumber = +1; }
}


