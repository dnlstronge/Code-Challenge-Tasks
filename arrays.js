// given an array of numbers 1 - 5, create a new array where each element is multiplied by two:

const numbers = [1, 2, 3, 4, 5];

const newNum = numbers.map( (e) => e*2 );

console.log(newNum);

//output: [2, 4, 6, 8, 10]
